# projeto_flask

Estou com o erro ao fazer o login depois que fiz o render depois do login não consigo ir à base e tambem tive alguns problemas com o terminal por causaa do comando pip install requiriments.text
